---
import { Button } from "@/components/ui/button";
import { GitHubLogoIcon } from "@radix-ui/react-icons";
import CurrentlyWorkingOn from "./currently-working-on.astro";
---

<section
  id="hero"
  class="h-screen max-h-[1080px] flex flex-col items-start md:items-center justify-center gap-8 p-6 relative anim-fade-in"
>
  <div
    class="flex flex-col items-start md:items-center justify-center gap-4 md:gap-2"
  >
    <CurrentlyWorkingOn />
    <h1
      class="font-display text-[48px] md:text-[56px] lg:text-[64px] leading-tight md:leading-relaxed text-left md:text-center font-semibold text-foreground"
    >
      Desenvolvedor & Designer de UX
    </h1>
    <p class="text-left md:text-center max-w-[840px] leading-relaxed">
      Me chamo Joel Vítor e este é meu site pessoal. Aqui você descobrirá o que
      eu faço, já fiz e ando fazendo enquanto na carreira de desenvolvedor de
      software.
    </p>
  </div>
  <div class="flex gap-4 justify-center">
    <a href="https://github.com/joevtap" target="_blank">
      <Button>
        <GitHubLogoIcon className="mr-2 h-4 w-4" />
        GitHub
      </Button>
    </a>
    <a href="/#about">
      <Button variant="secondary">Sobre mim</Button>
    </a>
  </div>
</section>

<style>
  section {
    @apply relative dark:border-none border-b-[1px] border-b-border;
    background: url(/bg.svg),
      radial-gradient(
        680px 480px at 50% 100%,
        rgba(255, 255, 255, 0.03) 0%,
        rgba(0, 0, 0, 0) 100%
      );

    background-position: center bottom;
    background-repeat: no-repeat;
    background-size: auto;
  }

  .anim-fade-in {
    animation: fade-in 300ms forwards;
  }

  @keyframes fade-in {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  section::after {
    @apply absolute bottom-0 left-0 right-0 h-[1px];
    content: "";
    background: radial-gradient(
      680px 480px at 50% 100%,
      rgba(255, 255, 255, 0.08) 0%,
      rgba(0, 0, 0, 0) 100%
    );
  }
</style>
